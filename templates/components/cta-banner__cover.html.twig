<div class="cta-banner cta-banner__cover{% if singleAsso %} cta-banner__asso{% endif %}">
    <div>
        {% block icone %}<i class="fab fa-discord"></i>{% endblock %}
    </div>
    <div>
        {% if singleAsso %}
        <div>
        {% endif %}
        <p class="title">{% block title %}#{% endblock %}</p>
        <p>{% block text %}#{% endblock %}</p>
        {% if singleAsso %}
            </div>
            <div>
        {% endif %}
        <a href="{% block url %}#{% endblock %}"{% if external %} target="_blank" rel="noopener noreferrer"{% endif %}>{% block urltext %}#{% endblock %} <i class="fas fa-external-link-alt"></i></a>
        {% if singleAsso %}
            </div>
        {% endif %}
    </div>
</div>