{% extends 'base.html.twig' %}

{% block title %}Forum des Associations{% endblock %}

{% block body %}

<h1>ICI SINGLE ASSO</h1>


    {% for association in associations %}
    <a href="{{ path('association', {slug: association.slug}) }}">
        <li>{{association.name}}</li>
    </a>
    {% endfor %}

{% endblock %}
